# Migration `20200410161354`

This migration has been generated by devnok at 4/10/2020, 4:13:54 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200410161100-..20200410161354
--- datamodel.dml
+++ datamodel.dml
@@ -3,16 +3,16 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 model AccountLink {
   facebookId String @unique
   id         Int    @default(autoincrement()) @id
   kakaoId    String @unique
-  userId String    @unique
+  userId     String @unique
   User       User   @relation(fields: [userId], references: [id])
 }
 model Coupon {
@@ -22,9 +22,9 @@
   id        Int      @default(autoincrement()) @id
   restId    Int
   servcie   String
   startAt   DateTime
-  userId String
+  userId    String
   warn      String
   Rest      Rest     @relation(fields: [restId], references: [id])
   User      User     @relation(fields: [userId], references: [id])
 }
@@ -45,13 +45,13 @@
   Rest   Rest   @relation(fields: [restId], references: [id])
 }
 model Like {
-  id     Int  @default(autoincrement()) @id
+  id     Int    @default(autoincrement()) @id
   restId Int
   userId String
-  Rest   Rest @relation(fields: [restId], references: [id])
-  User   User @relation(fields: [userId], references: [id])
+  Rest   Rest   @relation(fields: [restId], references: [id])
+  User   User   @relation(fields: [userId], references: [id])
 }
 model Location {
   latitude  Float
@@ -64,12 +64,12 @@
 model Profile {
   birthday DateTime?
   email    String?   @unique
-  gender   Gender?   @default(dbgenerated())
+  gender   Gender?
   id       Int       @default(autoincrement()) @id
   name     String?
-  userId String       @unique
+  userId   String    @unique
   User     User      @relation(fields: [userId], references: [id])
 }
 model Rest {
@@ -97,9 +97,9 @@
 }
 model User {
   createdAt   DateTime     @default(now())
-  id          String          @default(cuid()) @id
+  id          String       @id @default(cuid())
   nickname    String       @unique
   phone       String       @unique
   AccountLink AccountLink?
   Coupon      Coupon[]
```


