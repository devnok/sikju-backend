# Migration `20200416131025`

This migration has been generated by devnok at 4/16/2020, 1:10:25 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Rest" DROP COLUMN "location";
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200416131007..20200416131025
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 model AccountLink {
   facebookId String? @unique
@@ -110,5 +110,14 @@
 enum Gender {
   FEMALE
   MALE
+}
+model SpatialRefSy {
+  srid      Int     @id
+  authName  String? @map("auth_name")
+  authSrid  Int?    @map("auth_srid")
+  proj4text String?
+  srtext    String?
+
+  @@map("spatial_ref_sys")
 }
```


