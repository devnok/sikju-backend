# Migration `20200416141143`

This migration has been generated by devnok at 4/16/2020, 2:11:43 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200416140025..20200416141143
--- datamodel.dml
+++ datamodel.dml
@@ -3,16 +3,16 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 model AccountLink {
   facebookId String? @unique
   kakaoId    String? @unique
   userId     String  @unique
-  User       User    @relation(fields: [userId], references: [id])
+  user       User    @relation(fields: [userId], references: [id])
 }
 model Coupon {
   createdAt DateTime @default(now())
@@ -23,10 +23,10 @@
   service   String
   startAt   DateTime
   userId    String
   warn      String
-  Rest      Rest     @relation(fields: [restId], references: [id])
-  User      User     @relation(fields: [userId], references: [id])
+  rest      Rest     @relation(fields: [restId], references: [id])
+  user      User     @relation(fields: [userId], references: [id])
 }
 model Event {
   createdAt DateTime @default(now())
@@ -40,26 +40,26 @@
 model File {
   id     Int    @default(autoincrement()) @id
   restId Int
   url    String
-  Rest   Rest   @relation(fields: [restId], references: [id])
+  rest   Rest   @relation(fields: [restId], references: [id])
 }
 model Like {
   id     Int    @default(autoincrement()) @id
   restId Int
   userId String
-  Rest   Rest   @relation(fields: [restId], references: [id])
-  User   User   @relation(fields: [userId], references: [id])
+  rest   Rest   @relation(fields: [restId], references: [id])
+  user   User   @relation(fields: [userId], references: [id])
   @@unique([restId, userId])
 }
 model Location {
   latitude  Float
   longitude Float
   restId    Int   @unique
-  Rest      Rest  @relation(fields: [restId], references: [id])
+  rest      Rest  @relation(fields: [restId], references: [id])
 }
 model Profile {
   birthday DateTime?
@@ -67,9 +67,9 @@
   gender   Gender?
   id       Int       @default(autoincrement()) @id
   name     String?
   userId   String    @unique
-  User     User      @relation(fields: [userId], references: [id])
+  user     User      @relation(fields: [userId], references: [id])
 }
 model Rest {
   closeAt   DateTime?
@@ -91,9 +91,9 @@
   restId    Int     @unique
   service   String
   validTime Int     @default(168)
   warn      String?
-  Rest      Rest    @relation(fields: [restId], references: [id])
+  rest      Rest    @relation(fields: [restId], references: [id])
 }
 model User {
   createdAt   DateTime     @default(now())
```


