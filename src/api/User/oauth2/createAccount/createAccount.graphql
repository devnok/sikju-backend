type Mutation{
    createAccount(
        provider: Provider!
        id: String!
        phone: String!
        nickname: String!
        gender: Gender
        email: String
        name: String
    ): User!
}